---
// Hero section with avatar and typing animation
---

<section class="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-50 via-white to-purple-50 dark:from-gray-900 dark:via-blue-900/20 dark:to-purple-900/20 relative overflow-hidden">
  <!-- Animated Background -->
  <div class="absolute inset-0 bg-gradient-to-r from-primary/5 via-transparent to-purple-600/5 animate-pulse"></div>
  
  <!-- Floating Particles -->
  <div class="absolute inset-0 overflow-hidden">
    <div class="particle absolute w-2 h-2 bg-primary/20 rounded-full animate-float" style="left: 10%; animation-delay: 0s;"></div>
    <div class="particle absolute w-3 h-3 bg-purple-600/20 rounded-full animate-float" style="left: 20%; animation-delay: 2s;"></div>
    <div class="particle absolute w-1 h-1 bg-accent/30 rounded-full animate-float" style="left: 30%; animation-delay: 4s;"></div>
    <div class="particle absolute w-2 h-2 bg-primary/20 rounded-full animate-float" style="left: 40%; animation-delay: 1s;"></div>
    <div class="particle absolute w-3 h-3 bg-purple-600/20 rounded-full animate-float" style="left: 50%; animation-delay: 3s;"></div>
    <div class="particle absolute w-1 h-1 bg-accent/30 rounded-full animate-float" style="left: 60%; animation-delay: 5s;"></div>
    <div class="particle absolute w-2 h-2 bg-primary/20 rounded-full animate-float" style="left: 70%; animation-delay: 2s;"></div>
    <div class="particle absolute w-3 h-3 bg-purple-600/20 rounded-full animate-float" style="left: 80%; animation-delay: 4s;"></div>
    <div class="particle absolute w-1 h-1 bg-accent/30 rounded-full animate-float" style="left: 90%; animation-delay: 1s;"></div>
  </div>

  <div class="max-w-6xl mx-auto px-4 py-20 relative z-10">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <!-- Avatar Section -->
      <div class="flex justify-center lg:justify-start animate-fade-up">
        <div class="relative group">
          <div class="w-80 h-80 rounded-full bg-gradient-to-br from-primary to-purple-600 p-1 shadow-2xl group-hover:shadow-3xl transition-all duration-500">
            <div class="w-full h-full rounded-full bg-white dark:bg-gray-800 flex items-center justify-center overflow-hidden group-hover:scale-105 transition-transform duration-500">
              <img 
                src="/Kant_Nguyen_Astro_Blog/avatar-new.png" 
                alt="Kant Nguyễn" 
                class="w-full h-full object-cover rounded-full group-hover:rotate-3 transition-transform duration-500"
                onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';"
              />
              <div class="hidden w-full h-full bg-gradient-to-br from-primary/20 to-purple-600/20 flex items-center justify-center text-6xl font-bold text-primary">
                KN
              </div>
            </div>
          </div>
          <!-- Floating elements -->
          <div class="absolute -top-4 -right-4 w-8 h-8 bg-accent rounded-full animate-bounce"></div>
          <div class="absolute -bottom-4 -left-4 w-6 h-6 bg-primary rounded-full animate-pulse"></div>
          <div class="absolute top-1/2 -right-8 w-4 h-4 bg-purple-600 rounded-full animate-ping"></div>
        </div>
      </div>

      <!-- Text Section -->
      <div class="text-center lg:text-left animate-slide-left">
        <div class="mb-6">
          <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-4">
            <span class="gradient-text">Xin chào, tôi là Kant Nguyễn 👋</span>
          </h1>
          <div class="text-xl md:text-2xl lg:text-3xl text-gray-600 dark:text-gray-300 mb-8">
            <span id="typing-text" class="inline-block"></span>
            <span class="typing-cursor animate-pulse">|</span>
          </div>
        </div>

        <!-- Description -->
        <p class="text-lg text-gray-600 dark:text-gray-400 mb-8 max-w-2xl mx-auto lg:mx-0">
          Chuyên viên phát triển backend với niềm đam mê Java và mạng máy tính. 
          Sinh viên tiêu biểu HUTECH 2023 với kinh nghiệm thực tế trong các dự án tích hợp hệ thống.
        </p>

        <!-- Action Buttons -->
        <div class="flex flex-wrap gap-3 justify-center lg:justify-start">
          <a href="/Kant_Nguyen_Astro_Blog/about" 
             class="px-6 py-2.5 bg-primary text-white rounded-lg font-semibold hover:bg-primary/90 transform hover:scale-105 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
            </svg>
            Giới thiệu
          </a>
          <a href="/Kant_Nguyen_Astro_Blog/projects" 
             class="px-6 py-2.5 bg-accent text-gray-900 rounded-lg font-semibold hover:bg-accent/90 transform hover:scale-105 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
            </svg>
            Dự án
          </a>
          <a href="/Kant_Nguyen_Astro_Blog/blog" 
             class="px-6 py-2.5 border-2 border-primary text-primary dark:text-accent rounded-lg font-semibold hover:bg-primary hover:text-white dark:hover:bg-accent dark:hover:text-gray-900 transform hover:scale-105 transition-all duration-200 flex items-center gap-2 text-sm">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z"/>
            </svg>
            Blog
          </a>
          <a href="/Kant_Nguyen_Astro_Blog/documents/Nguyen Dang Khoi CV.pdf" 
             class="px-6 py-2.5 bg-gradient-to-r from-green-500 to-green-600 text-white rounded-lg font-semibold hover:from-green-600 hover:to-green-700 transform hover:scale-105 transition-all duration-200 shadow-lg hover:shadow-xl flex items-center gap-2 text-sm"
             target="_blank" rel="noopener noreferrer">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
            </svg>
            Tải CV
          </a>
        </div>

        <!-- Quick Stats with Count-up Animation -->
        <div class="mt-12 grid grid-cols-3 gap-8 max-w-md mx-auto lg:mx-0">
          <div class="text-center">
            <div class="text-2xl font-bold text-primary" data-count="3">0</div>
            <div class="text-sm text-gray-600 dark:text-gray-400">Dự án</div>
          </div>
          <div class="text-center">
            <div class="text-2xl font-bold text-primary" data-count="2">0</div>
            <div class="text-sm text-gray-600 dark:text-gray-400">Năm kinh nghiệm</div>
          </div>
          <div class="text-center">
            <div class="text-2xl font-bold text-primary" data-count="5">0</div>
            <div class="text-sm text-gray-600 dark:text-gray-400">Chứng chỉ</div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
    <svg class="w-6 h-6 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"/>
    </svg>
  </div>
</section>

<script>
  // Typing animation and count-up animation
  document.addEventListener('DOMContentLoaded', () => {
    // Typing animation
    const typingText = document.getElementById('typing-text');
    const text = "Backend Developer | Java Networking Enthusiast";
    let index = 0;
    
    function typeText() {
      if (index < text.length) {
        typingText.textContent += text.charAt(index);
        index++;
        setTimeout(typeText, 100);
      }
    }
    
    // Start typing animation after a short delay
    setTimeout(typeText, 1000);
    
    // Count-up animation
    function animateCountUp() {
      const counters = document.querySelectorAll('[data-count]');
      
      counters.forEach(counter => {
        const target = parseInt(counter.getAttribute('data-count'));
        const duration = 2000; // 2 seconds
        const increment = target / (duration / 16); // 60fps
        let current = 0;
        
        const timer = setInterval(() => {
          current += increment;
          if (current >= target) {
            current = target;
            clearInterval(timer);
          }
          counter.textContent = Math.floor(current);
        }, 16);
      });
    }
    
    // Start count-up animation when element is visible
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          animateCountUp();
          observer.unobserve(entry.target);
        }
      });
    });
    
    const statsSection = document.querySelector('.mt-12');
    if (statsSection) {
      observer.observe(statsSection);
    }
    
    // Smooth scroll for navigation links
    const links = document.querySelectorAll('a[href^="/"]');
    links.forEach(link => {
      link.addEventListener('click', (e) => {
        const href = link.getAttribute('href');
        if (href && href !== '#' && !href.includes('mailto:') && !href.includes('http')) {
          // For same-page navigation, try to find a section with matching ID
          if (href.startsWith('/#') || href === '/Kant_Nguyen_Astro_Blog/') {
            e.preventDefault();
            const targetId = href.replace('/Kant_Nguyen_Astro_Blog/', '').replace('#', '');
            const target = targetId ? document.getElementById(targetId) : document.body;
            if (target) {
              target.scrollIntoView({ behavior: 'smooth' });
            }
          }
          // For other paths, let the browser handle navigation normally
        }
      });
    });
  });
</script>
